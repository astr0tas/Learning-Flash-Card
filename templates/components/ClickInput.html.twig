<div class="flex gap-1.5 items-center select-none hover:opacity-80 active:opacity-70 {% if label is not defined or not label %} justify-center {% else %} cursor-pointer {% endif %}" {% if label is defined and label %} @click="$refs.click_input_{{ id|default(name) }}_ref.click();" {% endif %}>
  <input {% if type is not empty %} type="{{ type }}" {% endif %} {% if name|default(id) is not empty %} name="{{ name|default(id) }}" {% endif %} {% if id|default(name) is not empty %} id="{{ id|default(name) }}" {% endif %} {% if label is not empty %} aria-label="{{ label }}" {% endif %} {{ attributes.defaults({class:"cursor-pointer #{element_class}"} ) }} x-ref="click_input_{{ id|default(name) }}_ref" @click.stop/>
  {% if label is defined and label %}
    <label {% if name|default(id) is not empty %} for="{{ name|default(id) }}" {% endif %} class="select-none text-{{ size }} cursor-pointer" @click="$refs.click_input_{{ id|default(name) }}_ref.click();">
      {{ label }}
    </label>
  {% endif %}
</div>
